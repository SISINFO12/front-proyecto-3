// db.js
const mysql = require('mysql2');

const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',      
    password: '1234',  
    database: 'sys'    
});

db.connect(err => {
    if (err) console.log("Error de conexión a MySQL:", err.message);
    else console.log("Carros2 - CONECTADO A MYSQL (BD: sys)");
});

module.exports = db;